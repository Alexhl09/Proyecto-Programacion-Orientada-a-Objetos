    /*Nombre:createNave
*/
    /*Nombre:createEnemigo
*/
    /* @funcName createEnemigoMedioMuerto
      @desc Se crea el ovni mediomuerto, su tamño,posicion innicial, cuerpo fisico,se le asigna bitMask, NO afectado por la gravedad, NO es dinamico y se mencionan los objetos con los que puede colisionar.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /* @funcName createEnemigo2Arriba
     @desc Se crea el ovni 2 de Arriba, su tamño,posicion innicial, cuerpo fisico,se le asigna bitMask, NO afectado por la gravedad, NO es dinamico y se mencionan los objetos con los que puede colisionar.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createEnemigo2Abajo
     @desc Se crea el ovni 2 de abajo, su tamño,posicion innicial, cuerpo fisico,se le asigna bitMask, NO afectado por la gravedad, NO es dinamico y se mencionan los objetos con los que puede colisionar.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /* @funcName createEnemigo2ArribaMedioMuerto
     @desc Se crea el ovni mediomuerto 2 de arriba, su tamño,posicion innicial, cuerpo fisico,se le asigna bitMask, NO afectado por la gravedad, NO es dinamico y se mencionan los objetos con los que puede colisionar.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createEnemigo2AbajoMedioMuerto
     @desc Se crea el ovni mediomuerto 2 de abajo, su tamño,posicion inicial, cuerpo físico,se le asigna bitMask, NO afectado por la gravedad, NO es dinámico y se mencionan los objetos con los que puede colisionar.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createEnemigoGrande
     @desc Se crea el ovni Grande,su tamño,posición inicial, cuerpo físico,se le asigna bitMask, NO afectado por la gravedad, NO es dinámico y se mencionan los objetos con los que puede colisionar, y aparece depsués de 10 seg.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName crearEtiqueta
     @desc Se crea el score como Label, con valor inicial de 0, se coloca en una posición fija y se modifican sus propiedades del texto.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName crearAnimacionDeAsteroides
     @desc Se crea los dos tipos de asteroides en intervalos de tiempo y tambien de las moendas y las vidas
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName crearScene
     @desc Se crea el escenario y se llaman todas las funciones que crean los spriteNodes iniciales
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createNave2
     @desc  Se crea la nave 2,su tamño,posición inicial, cuerpo físico,se le asigna bitMask,afectado por la gravedad, es dinámico y se mencionan los objetos con los que puede colisionar, y aparece depsués de 10 seg.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createNave3
     @desc  Se crea la nave 2,su tamaño,posición inicial, cuerpo físico,se le asigna bitMask,afectado por la gravedad, es dinámico y se mencionan los objetos con los que puede colisionar, y aparece depsués de 10 seg.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createNuevoLaser
     @desc Crea nuevo laser de miNave y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createNuevoLaserForNaveMuerta
     @desc Crea laser para miNave muerta
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createNuevoLaserForNaveMuerta
     @desc Crea laser para miNave muerta
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createLaserOvni
     @desc Funcion objective c que crea nuevo laser de ovni y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createLaserOvniMedioMuerto
     @desc funcion objective c que crea los laser y sus propiedades.
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createLaserOvniArriba
     @desc Crea nuevo laser de OvniArriba y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /* @funcName createLaserOvniAbajo
     @desc Crea nuevo laser de OvniAbajo y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createLaserOvniArriba
     @desc Crea nuevo laser de OvniArriba y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createLaserOvniAbajoMedioMuerto
     @desc Crea nuevo laser de Ovni abajo medio muerto y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createLaserOvniGrande
     @desc Crea nuevo laser de Ovni Grande y sus propiedades
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcName createNuevosAsteroides
     @desc Crea otro asteroide en forma de sprite, y los coloca de forma aleatroia
     @param Sin parametros
     @return Sin valor de retorno
*/
    /*@funcname update
     @desc Actualiza el juego modificando los tiempos en que se crean los asteroides, y hace que el ovni persiga a miNave
     @param Sin parametros
     @return Sin valor de retorno
*/
